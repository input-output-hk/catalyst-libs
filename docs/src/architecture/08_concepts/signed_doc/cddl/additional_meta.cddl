additional_fields = {
  ? "ref" => ref_type,
  ? "rev-hash" => ref_hash_type,
  ? "template" => ref_type,
  ? "reply" => ref_type,
  ? "section" => text,
  ? "collabs" => [+any],
}

ref_type = UUID / [UUID, UUID]
ref_hash_type = [ref_type, hash-bytes]

UUID = #6.37(bytes)

;# include hash
