# `proptest` entities

Collection of implementation
[`proptest::arbitrary::Arbitrary`](https://docs.rs/proptest/latest/proptest/arbitrary/trait.Arbitrary.html) trait
for different types from different crates from this repo.
It is only used for test purposes.
